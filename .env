#!/bin/bash

export KUBE_VERSION=v1.35.0
export CORE_VERSION=v1.12.4          # pick a cluster-api core version
export INFRA_PROVIDER=docker         # e.g., docker, aws, vsphere, etc.
export INFRA_VERSION=v1.12.4         # pick matching infra provider version
export BOOTSTRAP_VERSION=v1.12.4     # bootstrap provider version (kubeadm)
export CONTROL_PLANE_MACHINE_TYPE=control-plane
export WORKER_MACHINE_TYPE=worker
